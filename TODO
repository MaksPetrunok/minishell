0. Add support of local variables.
	(add export builtin to make local variable environment variable)

1. Make function which iterates over tokens in COMMAND node and:
	- if there is T_ASSIGN tokens - assign corresponding variables
	- makes argument array from T_WORD tokens
	- makes redirection list from T_IO_NUM + T_WORD tokens

2. If there is no words in argument array - skip redirectioin.
3. Else accomplish all redirection in specified sequense (!) and try to launch new process from args.

4. Make function which performs expansions.




REMOVE tree.c

Command graammar:

complete_command		: command_redir
						| assign command_redir

command_redir			: command
						| redir
						| command_redir command
						| command_redir	redir

assign					: assign T_ASSIGN
						| T_ASSIGN

redir					: redir redir_num
						| redir_num

redir_num				: T_IO_NUM T_WORD

command					: command T_WORD
						| T_WORD

