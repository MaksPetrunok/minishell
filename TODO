Autocompletion:
After ".....;/" searches binary files in PATH instead of the root.

When press TAB after
prompt: echo;(space)
segfault occurs.

Clarify behaviour when more rows is being printed than can
fit into window.

